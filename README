This project uses different document formats to transform a document of one format to another. 
The focus is on formats that are commonly used to store or present documents produced by OCR engines.
For more information about the project and its modules, install maven version 3 or later 
and run 'mvn compile site-deploy' in the project directory. The site will be created in target/site-deploy
directory.